import 'package:flutter/material.dart';

class ShalatApp extends StatelessWidget {
  const ShalatApp({super.key});

  @override
  Widget build(final BuildContext context) {
    return MaterialApp();
  }
}
